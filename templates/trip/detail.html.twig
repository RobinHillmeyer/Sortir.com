{% extends 'base.html.twig' %}

{% block body %}
    <h2>Afficher</h2>
    <p>Nom de la Sortie : {{ trip.name }}</p>
    <p>Date et heure de la sortie : {{ trip.startDateTime | date('d-m-Y') }}</p>
    <p>Date limite d'inscription : {{ trip.registrationDeadLine | date('d-m-Y') }}</p>
    <p>Nombre de places : {{ trip.registrationNumberMax }}</p>
    <p>Durée : {{ trip.duration | date('d-m-Y') }}</p>
    <p>Description : {{ trip.information }}</p>
    <p>Etat : {{ trip.state }}</p>
{% endblock %}

{% block title %}
    Detail de la Sortie
{% endblock %}